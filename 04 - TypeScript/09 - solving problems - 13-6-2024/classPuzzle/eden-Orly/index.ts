function smallesttolargest(n1: number, n2: number, n3: number, n4: number): number | undefined {
    try {
        /*let smallest:number|undefined = n1;
        let small:number|undefined = undefined;
        let large:number|undefined = undefined;
        let larger:number|undefined = undefined;
        let placeholder:number|undefined = 0;
        if (smallest >n2) {
            small = smallest;
            smallest = n2;
        } else if (smallest>n3){
            placeholder = small;
            small = smallest;
            smallest = n3;
            if(placeholder < small){
                large = small;
                small = placeholder;
            } else {
                large = placeholder;
            }
        } else if (smallest>)*/
        const numbers = [];
        while(numbers.length<4){
            
        }
    } catch (error) {
        console.error(error);
        return undefined
    }
}